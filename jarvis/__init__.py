"""Jarvis — a personal main agent with capability-scoped subagents.

The public surface is intentionally tiny. `brain.py` is the only module that
imports Tau; everything else speaks Jarvis's own types.
"""

__version__ = "0.0.1"

__all__ = ["__version__"]
